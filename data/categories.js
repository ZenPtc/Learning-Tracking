/* ============================================================
   Learning Journal — categories
   ------------------------------------------------------------
   The list of categories used to group entries. This lives in
   the project folder (not the browser), so it's portable and
   the same on every machine.

   Managed by Claude: just ask, e.g. "add a Design category in
   teal" or "rename Math to Mathematics". To do it by hand, add
   an object below:
     { name: "Programming", color: "#8aa6ff" }
   The "name" should match the "category" field used by entries
   in data/entries.js.
   ============================================================ */
window.CATEGORIES = [
  { name: "Dev Tools", color: "#8aa6ff" }
];
